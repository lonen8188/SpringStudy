-- 프로젝트에 ojdbc8.jar 연결 한 후에 진행한다.
-- 프로젝트 우클릭 -> Build Path -> config build path -> Libaries 탭 -> Classpath에 삽입 -> apply
-- Deployment Assembly -> java build path -> ojdbc8.jar 추가 -> apply
-- 결과 Referenced Libraries에 ojdbc6.jar 보이면 성공 p80

